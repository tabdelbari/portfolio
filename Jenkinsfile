pipeline {
    agent {
        dockerfile {
            filename 'Dockerfile'
            dir 'build'
        }
    }

    stages {
        stage('Build') {
            steps {
                echo 'Done.'
            }
        }
    }
}
